import styled from "styled-components";

import * as S from "../SignUp/SignUp.style";

export const container = S.outerContainer;

export const input = S.input;

export const submit = S.submit;

export const header = S.header;
